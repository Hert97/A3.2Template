#version 450

layout (local_size_x = 16, local_size_y = 16) in;
layout (binding = 0, rgba8) uniform image2D resultImage;

layout (binding = 1) buffer HistoEq
{
	uint histoBin[256];
    float cdf[256];

} histoEq;

void main()
{

  imageStore(resultImage, ivec2(gl_GlobalInvocationID.xy), vec4(vec3(histoEq.histoBin[0]),1.0));
}
 	
 
 
 
